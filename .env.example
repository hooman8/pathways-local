# Optional for npm run dev; Compose supplies its own configuration.
APP_BASE_URL=http://localhost:5173
PATHWAYS_DB_PATH=./data/pathways.sqlite
NEXT_TELEMETRY_DISABLED=1
# Docker port override: PATHWAYS_PORT=8081 docker compose up -d --build
